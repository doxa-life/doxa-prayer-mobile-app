<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Required for all network access. Flutter auto-injects this for debug/
         profile builds only, so it must be declared here for release builds. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <!-- User-grantable exact-alarm permission so prayer reminders can fire at the
         chosen minute. Deliberately NOT USE_EXACT_ALARM: that variant is auto-granted
         but Play-policy-restricted to alarm/calendar apps, which this content app is
         not. When this is not granted we fall back to inexact scheduling — see
         rescheduleAllReminders in reminders_notifications.dart. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <application
        android:label="@string/app_name"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:enableOnBackInvokedCallback="false">
        <!-- enableOnBackInvokedCallback=false opts out of Android 13+ predictive back.
             Predictive back gates whether the framework handles back via
             SystemNavigator.setFrameworkHandlesBack, and with StatefulShellRoute's nested
             navigators that flag loses a first-frame race (a single-screen branch reports
             canHandlePop=false), so the shell PopScope isn't consulted until after the first
             navigation — back would exit the app instead of returning Home. Opting out routes
             every back press through handlePopRoute()/didPopRoute(), so the shell PopScope is
             consulted from the first frame. See lib/app_shell.dart. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Verified App Link: "Pray on the app" smart links (https://pray.doxa.life/app/<slug>).
                 Requires public/.well-known/assetlinks.json on the host to auto-verify. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https" android:host="pray.doxa.life" android:pathPrefix="/app"/>
            </intent-filter>
            <!-- Verified App Link: prayer deep links (https://pray.doxa.life/<slug>/prayer[/<date>])
                 that mirror the campaigns site's prayer pages and open the Pray screen. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https" android:host="pray.doxa.life" android:pathPattern="/.*/prayer"/>
                <data android:scheme="https" android:host="pray.doxa.life" android:pathPattern="/.*/prayer/.*"/>
            </intent-filter>
            <!-- Custom scheme fallback (doxa:///app/<slug>) used by the web smart-link page
                 when App Links aren't verified. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="doxa"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
