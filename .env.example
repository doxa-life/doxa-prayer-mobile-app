ANON_SIGNUP_SECRET=your_secret_token_here

# Optional API base URL override. When unset, debug builds use the local dev
# server (http://localhost:3000) and release builds use https://pray.doxa.life.
# Set this to point a debug build at the live site, or at a LAN IP.
#   API_BASE_URL=https://pray.doxa.life
